/// <summary>
/// UI-level binding target. Keyboard and mouse share the KeyboardMouse control
/// scheme, but the settings screen exposes them as separate Elden Ring-style columns.
/// </summary>
public enum InputBindingTarget
{
    Keyboard,
    Mouse,
    Gamepad
}
